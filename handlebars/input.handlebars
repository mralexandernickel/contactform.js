<div class="form-group">
  <label for="input_{{name}}" class="col-lg-2 control-label">{{name}}</label>
  <div class="col-lg-10">
    <input type="{{type}}" class="form-control" id="input_{{name}}" placeholder="{{name}}">
  </div>
</div>